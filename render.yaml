services:
  - type: web
    name: mini-ide
    runtime: python
    plan: free
    buildCommand: "apt-get update && apt-get install -y clang || apt update && apt install -y clang; echo clang ready"
    startCommand: python3 server.py